source "https://rubygems.org"
ruby "~> 3.3"

gem "rails", "~> 8.0"
gem "pg", "~> 1.5"
gem "puma", "~> 6.4"
gem "rack-cors"
gem "bcrypt", "~> 3.1"
gem "bootsnap", "~> 1.18", require: false

# Auth
gem "jwt", "~> 2.8"

# Background jobs
gem "sidekiq", "~> 7.3"
gem "sidekiq-scheduler", "~> 6.0"
gem "redis", "~> 5.3"
# Pin connection_pool to 2.x — Sidekiq 7.3 calls TimedStack#pop(timeout) but
# connection_pool 3.0 removed positional args, breaking `initial_wait`:
#   ArgumentError: wrong number of arguments (given 1, expected 0)
# Sidekiq 7.4+ adopts the 3.0 API; until we bump Sidekiq, hold connection_pool back.
gem "connection_pool", "~> 2.4"

# Storage (MinIO / S3-compatible)
gem "aws-sdk-s3", "~> 1.160"

# HTTP client (for Anthropic API, WooCommerce adapter, webhooks)
gem "faraday", "~> 2.9"
gem "faraday-retry"

# Email
gem "resend", "~> 1.3"

# Billing
gem "stripe", "~> 12.0"

# Serialization
gem "oj", "~> 3.16"

# Validation
gem "dry-validation", "~> 1.10"

# Vector embeddings (pgvector for Rails)
gem "neighbor", "~> 0.5"

# Pagination
gem "pagy", "~> 9.0"

# Monitoring
gem "sentry-ruby", "~> 5.18"
gem "sentry-rails", "~> 5.18"

group :development, :test do
  gem "rspec-rails", "~> 7.0"
  gem "factory_bot_rails", "~> 6.4"
  gem "faker", "~> 3.4"
  gem "shoulda-matchers", "~> 6.4"
  gem "database_cleaner-active_record", "~> 2.2"
  gem "rubocop-rails-omakase", require: false
end

group :development do
  gem "annotaterb", "~> 4.13"
  gem "bullet", "~> 8.0"
end
